pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "./test.sh"
            }
        }
        stage('SendNotification') {
                
        }
}
}
